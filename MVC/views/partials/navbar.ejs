<!-- Botão de abrir o menu -->
<div class="menu-icon fixed top-6 right-6 z-50 cursor-pointer group" id="menuButton">
  <div class="relative w-8 h-6">
    <span class="absolute block w-full h-[2px] bg-white rounded-full transition-all duration-300 top-0 
      group-[.active]:top-1/2 group-[.active]:-translate-y-1/2 group-[.active]:rotate-45">
    </span>
    <span class="absolute block w-full h-[2px] bg-white rounded-full transition-all duration-300 top-1/2 
      -translate-y-1/2 group-[.active]:opacity-0">
    </span>
    <span class="absolute block w-full h-[2px] bg-white rounded-full transition-all duration-300 bottom-0 
      group-[.active]:top-1/2 group-[.active]:-translate-y-1/2 group-[.active]:-rotate-45">
    </span>
  </div>
</div>


<!-- Menu lateral -->
<nav class="fixed top-0 right-0 h-full w-[280px] md:w-[580px] 
    bg-gray-900/80 backdrop-blur-lg text-white z-40 
    transform translate-x-full transition-transform duration-300 ease-in-out  shadow-xl" 
    id="navMenu">

  <ul class="pt-28 px-10 space-y-8">
    <li>
      <a href="#" class="block text-xl font-semibold tracking-widest hover:text-pink-400 
      transition-colors duration-300">
        HOME
      </a>
    </li>
    <li>
      <a href="#" class="block text-xl font-semibold tracking-widest hover:text-pink-400 
      transition-colors duration-300">
        SOBRE
      </a>
    </li>
    <li>
      <a href="#" class="block text-xl font-semibold tracking-widest hover:text-pink-400 
      transition-colors duration-300">
        GALERIA
      </a>
    </li>
    <li>
      <a href="#" class="block text-xl font-semibold tracking-widest hover:text-pink-400 
      transition-colors duration-300">
        CONTATO
      </a>
    </li>
  </ul>

  <div class="absolute bottom-6 left-10 text-xs text-gray-400">
    <p>🌐 Português do Brasil</p>
  </div>
</nav>


<!-- Fundo escuro ao abrir o menu -->
<div class="overlay fixed top-0 left-0 w-full h-full bg-black/50 z-30 hidden backdrop-blur-sm" id="overlay"></div>




<script>
  const menuButton = document.getElementById('menuButton');
  const navMenu = document.getElementById('navMenu');
  const overlay = document.getElementById('overlay');

  function toggleMenu() {
    const isOpen = navMenu.classList.contains('translate-x-0');
    if (isOpen) {
      // Fecha o menu
      navMenu.classList.remove('translate-x-0');
      navMenu.classList.add('translate-x-full');
      overlay.classList.add('hidden');
      menuButton.classList.remove('active');
    } else {
      // Abre o menu
      navMenu.classList.remove('translate-x-full');
      navMenu.classList.add('translate-x-0');
      overlay.classList.remove('hidden');
      menuButton.classList.add('active');
    }
  }

  function closeMenu() {
    navMenu.classList.remove('translate-x-0');
    navMenu.classList.add('translate-x-full');
    overlay.classList.add('hidden');
    menuButton.classList.remove('active');
  }

  menuButton.addEventListener('click', toggleMenu);
  overlay.addEventListener('click', closeMenu);
</script>
