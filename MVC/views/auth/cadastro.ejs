<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastro - GTA VI</title>
  <link rel="shortcut icon" href="/images/logo-nav.webp" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/daisyui@3.9.4/dist/full.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/login.css">
</head>
<body class="h-screen overflow-hidden bg-gray-900 text-white">
  
  <% if (typeof success !== 'undefined' && success) { %>
    <div class="p-4 mb-4 text-sm text-green-700 bg-green-100 rounded-lg">
      <%= success %>
    </div>
  <% } %>

  <div class="flex h-full">
      <!-- Lado esquerdo -->
<div class="hidden md:flex w-1/2 items-center justify-center left-login-container relative overflow-hidden">
    <!-- Vídeo de fundo -->
    <video autoplay muted loop class="absolute z-0 w-auto min-w-full min-h-full max-w-none object-cover">
      <source src="/images/m2-res_1080p.mp4" type="video/mp4">
      Seu navegador não suporta vídeos HTML5.
    </video>
    
    <!-- Overlay escuro para melhor legibilidade -->
    <div class="absolute inset-0 bg-black bg-opacity-50 z-1"></div>
    
    <div class="text-center p-8 max-w-md relative z-10 animate-fade-in-left">
      <img src="/images/gta6-logo.png" alt="GTA VI Logo" class="w-64 mx-auto mb-5 gta-logo">
      <h1 class="text-4xl font-bold mb-4 text-white pulsing-text">Bem-vindo de volta</h1>
      <p class="text-xl text-gray-200 opacity-90 mb-6">Entre na sua conta para acessar conteúdo exclusivo sobre GTA VI.</p>
      <div class="mt-8">
        <div class="flex justify-center space-x-4">
          <button class="btn btn-circle btn-outline text-white hover:bg-white hover:text-blue-600 social-btn">
            <i class="fab fa-facebook-f"></i>
          </button>
          <button class="btn btn-circle btn-outline text-white hover:bg-white hover:text-blue-400 social-btn">
            <i class="fab fa-twitter"></i>
          </button>
          <button class="btn btn-circle btn-outline text-white hover:bg-white hover:text-red-500 social-btn">
            <i class="fab fa-google"></i>
          </button>
        </div>
      </div>
    </div>
  </div>

    <!-- Lado direito - Formulário de Cadastro -->
    <div class="w-full md:w-1/2 flex items-center justify-center p-5 animate-fade-in-right">
      <div class="w-full max-w-md p-8 space-y-6 login-form-container rounded-xl relative overflow-hidden">
        <!-- Efeito de partículas sutis -->
        <div class="absolute inset-0 opacity-10 z-0" id="particles-js"></div>
        
        <div class="text-center relative z-10">
          <img src="/images/logo-nav.webp" alt="GTA VI Logo" class="w-16 mx-auto mb-4">
          <h2 class="text-3xl font-bold text-red-500 login-title">REGISTRE-SE</h2>
          <p class="text-gray-300 login-subtitle">Crie sua conta do Social Club</p>
        </div>

        <!-- Mensagens de erro/sucesso -->
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="p-4 mb-4 text-sm text-red-700 bg-red-100 rounded-lg relative z-10">
            <%= error %>
          </div>
        <% } %>

        <form action="/cadastro" method="post" class="space-y-6 relative z-10">
          <!-- Usuário -->
          <div class="space-y-2">
            <label class="block text-sm font-medium text-gray-300 transition-all duration-300">Nome de Usuário</label>
            <div class="relative">
              <input type="text" name="usuario" id="usuario" placeholder="Seu nome de usuário" 
                     value="<%= typeof usuario !== 'undefined' ? usuario : '' %>"
                     class="w-full px-4 py-3 login-input border border-gray-600 rounded-lg focus:outline-none text-white placeholder-gray-400"
                     required>
              <div class="absolute inset-y-0 right-0 flex items-center pr-3 pointer-events-none">
                <i class="far fa-user text-gray-400"></i>
              </div>
            </div>
          </div>

          <!-- Senha -->
          <div class="space-y-2">
            <label class="block text-sm font-medium text-gray-300 transition-all duration-300">Senha</label>
            <div class="relative">
              <input type="password" id="senha" name="senha" placeholder="••••••••" 
                     class="w-full px-4 py-3 login-input border border-gray-600 rounded-lg focus:outline-none text-white placeholder-gray-400"
                     required>
              <div class="absolute inset-y-0 right-0 flex items-center pr-3">
                <i class="far fa-eye text-gray-400 hover:text-red-400 cursor-pointer toggle-password"></i>
              </div>
            </div>
          </div>

          <!-- Confirmar Senha -->
          <div class="space-y-2">
            <label class="block text-sm font-medium text-gray-300 transition-all duration-300">Confirmar Senha</label>
            <div class="relative">
              <input type="password" id="confirmar_senha" name="confirmar_senha" placeholder="••••••••" 
                     class="w-full px-4 py-3 login-input border border-gray-600 rounded-lg focus:outline-none text-white placeholder-gray-400"
                     required>
              <div class="absolute inset-y-0 right-0 flex items-center pr-3">
                <i class="far fa-eye text-gray-400 hover:text-red-400 cursor-pointer toggle-password"></i>
              </div>
            </div>
          </div>

          <!-- Termos e Condições -->
          <div class="flex items-center">
            <input type="checkbox" id="termos" name="termos" class="checkbox checkbox-sm checkbox-primary bg-gray-600 border-gray-500" required>
            <label for="termos" class="ml-2 text-sm text-gray-300 hover:text-gray-200 cursor-pointer">
              Eu concordo com os <a href="#" class="text-red-400 hover:text-red-300">Termos de Serviço</a> e <a href="#" class="text-red-400 hover:text-red-300">Política de Privacidade</a>
            </label>
          </div>

          <!-- Botão de Cadastro -->
          <button type="submit" class="w-full btn login-btn bg-red-600 hover:bg-red-700 border-none py-3 text-lg font-semibold">
            <i class="fas fa-user-plus mr-2"></i> CADASTRAR
          </button>
        </form>

        <!-- Link para Login -->
        <div class="text-center text-sm text-gray-400 relative z-10">
          Já tem uma conta? <a href="/login" class="text-red-400 hover:text-red-300 ml-1 font-medium transition-colors">Faça login</a>
        </div>

        <!-- Divisor -->
        <div class="divider text-gray-500 my-6 relative z-10">CONECTE-SE COM</div>

        <!-- Botões Sociais -->
        <div class="flex justify-center space-x-4 relative z-10">
          <button class="btn social-btn btn-outline text-white hover:bg-white hover:text-gray-900 border-gray-600">
            <i class="fab fa-google mr-2"></i> Google
          </button>
          <button class="btn social-btn btn-outline text-white hover:bg-white hover:text-gray-900 border-gray-600">
            <i class="fas fa-gamepad mr-2"></i> Social Club
          </button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
  <script src="/js/login.js"></script>
</body>
</html>
